(async()=>{const box=document.querySelector('#timeline-list');if(!box)return;try{const d=await fetch('research-data.json').then(r=>r.json());const rows=(d.sources||[]).slice().sort((a,b)=>(Number(a.date_start)||0)-(Number(b.date_start)||0));const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));box.innerHTML=rows.map(s=>`<article class="card" style="margin-bottom:14px"><div><span class="grade grade-${esc(s.evidence_grade)}">${esc(s.evidence_grade)}</span> <span class="muted small">${esc(s.date_label||'')}</span></div><h3><a href="source.html?id=${encodeURIComponent(s.source_id)}">${esc(s.title)}</a></h3><p>${esc(s.historical_claim||'')}</p><p class="small muted">${esc(s.civilization||'')} · ${esc(s.region||'')} · ${esc(s.verification_status||'')}</p></article>`).join('')||'<p>কোনো উৎস-রেকর্ড পাওয়া যায়নি।</p>'}catch(e){box.innerHTML='<div class="callout"><strong>কালপঞ্জির ডেটা লোড করা যায়নি।</strong><p>উৎস-রেকর্ড সরাসরি দেখতে গবেষণা-সংগ্রহ খুলুন।</p></div>';console.error(e)}})();
